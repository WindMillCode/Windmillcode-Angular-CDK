import { WMLDragNDropDirective } from './wml-drag-n-drop.directive';

describe('WMLDragNDropDirective', () => {
  it('should create an instance', () => {
    const directive = new WMLDragNDropDirective();
    expect(directive).toBeTruthy();
  });
});
